<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 둥근 모서리 설정 -->
    <corners android:radius="16dp" />
    <solid android:color="#F55424" />
    <!-- 버튼 테두리 설정 (선 색상 및 두께) -->
    <stroke
        android:width="2dp"
        android:color="#FFFFFF" />
</shape>
